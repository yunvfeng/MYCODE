#include <stdio.h>
#include <string.h>
int main()
{
    char arr[] = {"i love you so~~~"};
    
    int size = strlen(arr);
    int left = 0,right = size -1;
    printf("%d",size);
    


    return 0;
}